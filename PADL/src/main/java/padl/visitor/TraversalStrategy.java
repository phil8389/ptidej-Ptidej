package padl.visitor;
// ENUM to select the type of traversal for the visitor
public enum TraversalStrategy {
    DFS,           // Depth-First Search
    BFS,           // Breadth-First Search
    Selective,      // Selective
}
